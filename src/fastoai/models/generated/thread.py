# Generated by FastOAI, DON'T EDIT
from typing import Annotated, Literal, Optional

from openai._models import BaseModel
from openai.types.beta.thread import ToolResources
from sqlmodel import Field, SQLModel

from .._types import as_sa_type


class ThreadBase(BaseModel):
    created_at: int
    """The Unix timestamp (in seconds) for when the thread was created."""

    tool_resources: Annotated[Optional[ToolResources], Field(sa_type=as_sa_type(ToolResources), nullable=True)] = None
    """
    A set of resources that are made available to the assistant's tools in this
    thread. The resources are specific to the type of tool. For example, the
    `code_interpreter` tool requires a list of file IDs, while the `file_search`
    tool requires a list of vector store IDs.
    """


class Thread(ThreadBase, SQLModel, table=True):
    id: Annotated[str, Field(primary_key=True)]
    """The identifier, which can be referenced in API endpoints."""


class ThreadPublic(ThreadBase):
    id: str
    """The identifier, which can be referenced in API endpoints."""
    metadata: Optional[object] = None
    """Set of 16 key-value pairs that can be attached to an object.

    This can be useful for storing additional information about the object in a
    structured format. Keys can be a maximum of 64 characters long and values can be
    a maximum of 512 characters long.
    """
    object: Literal["thread"]
    """The object type, which is always `thread`."""
